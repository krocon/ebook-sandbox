version: "3.9"
services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - prometheus-data:/prometheus
#    environment:
#      DEMOFOLDER: "./node_modules"
volumes:
  prometheus-data:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: /data/fotos/Beispiele